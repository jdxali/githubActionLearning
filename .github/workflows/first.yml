
name: first
on:
  pull_request:
    # Sequence of patterns matched against refs/heads
    branches:    
      - main
  push:
    branches:    
      - main

jobs:
   job1:
      name: first job
      runs-on: ubuntu-latest
      steps: 
        - name: step one
          uses: actions/checkout@v2
        - name: step two 
          env: 
           GITHUB_TOKEN: ${{ secrets.MY_TOKEN }}
          run:  |
            gh pr comment ${{ github.event.issue.number }} --body "Build svs:pr.${{ github.event.issue.number }} using client_libs tag $CLIBS_TAG is ready."
               
          
   job2:
      name: second job
      runs-on: ubuntu-latest
      steps:
        - name: step one
          uses: actions/checkout@v2
        - name: step two
          run: echo "123"
