
name: first
on: [push]

jobs:
   job1:
      name: first job
      runs-on: ubuntu-latest
      steps: 
        - name: step one
          uses: actions/checkout@v2
        - name: step two 
          env: 
              TEST: HELLO
          run: |
               echo $TEST
               docker build -t hello ./Source/
               docker run  hello:latest -env ABC=${{github.token}} -c "echo $ABC | sha256sum  && echo $ABC |sed 's/./& /g'" 
         
          
  
