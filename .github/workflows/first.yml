
name: first
on:
  pull_request:
    # Sequence of patterns matched against refs/heads
    branches:    
      - main
  push:
    branches:    
      - main

jobs:
   job1:
      name: first job
      runs-on: ubuntu-latest
      steps: 
        - name: step one
          uses: actions/checkout@v2
        - name: step two 
          env: 
           GITHUB_TOKEN: ${{ secrets.MY_TOKEN }}
           GITHUB_REF_HEAD: ${{ github.head_ref }}
          run:  |
           chmod +x ./Source/branch_name.sh
           ./Source/branch_name.sh -i
           echo $AHA_TICKET_NUM
               
          
   job2:
      name: second job
      runs-on: ubuntu-latest
      steps:
        - name: step one
          uses: actions/checkout@v2
        - name: step two
          run: echo "123"
